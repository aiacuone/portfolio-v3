import CheapsharkProjectHeader from './CheapsharkProjectHeader'
import NoteTakerProjectHeader from './NoteTakerProjectHeader'
import TekkenNetProjectHeader from './TekkenNetProjectHeader'

export const projectHeaders = {
  cheapshark: <CheapsharkProjectHeader />,
  noteTaker: <NoteTakerProjectHeader />,
  tekkenNet: <TekkenNetProjectHeader />,
}
